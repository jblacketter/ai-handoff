{"round": 1, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Phase 43 implementation \u2014 cockpit hardening, a legible cycle. Branch phase-43-cockpit-hardening @ f341913 (PR to follow). gate: see entry @ f341913.\n\nWhat changed, where:\n- tagteam/cockpit_api.py \u2014 Phase 43 read model: OUTCOMES + normalize_outcome; activity_payload() (in-flight marker + usage + conversation_turns + gates + panels + non-succeeded launches \u2192 newest first, running first; ids `turn:<stem>` for stem-bearing turns so the running row and its later record are ONE row, else `<source>:<rowid>`; server-side dedupe, terminal record wins; a launch that reached its lead turn is shown as that conversation row); last_turn(); launch_view() (effective status derived from the lead turn / owner liveness; failed only within 24 h and for the current intent); now_payload gains turn_kind / launch / last_turn; events_signature adds conversation_turns + launches digests and in-flight log growth in LOG_SIGNAL_STEP (8 KiB) steps; turn_log_path() (stem validated, strictly under turns dir); tail_payload(stem=).\n- tagteam/server.py \u2014 GET /api/activity[?limit]; GET /api/activity/log/<stem>/events (SSE over .tagteam/turns/<stem>.log: `line` per complete line with id = byte offset, Last-Event-ID / ?after= replay, a final partial line flushed once the marker is gone, `end` when no marker names the stem and the file is drained; 400 bad stem, 404 unknown); _stream_sse() shared poller \u2014 lead_sse now rides it (ids/replay/end/heartbeat/--max-sse identical); /api/tail?stem=.\n- tagteam/data/web/cockpit.html|js|css \u2014 Cycle region (#cycle: Lead/Reviewer lanes, token on the owed side, running lane pulses, kind named, status line with last outcome) + Activity log (#activity: rows keyed by id, patched never wiped; running row streams via the log SSE or the shared conversation stream; finished rows stay with OUTCOME_LABEL status, duration, [log]/[transcript]; [cancel] on the running row \u2192 cancel-turn / lead cancel; a running row whose record vanishes is marked orphaned); Start \u2192 starting state from now.launch (Start card suppressed while pending, back with the failure when failed); #chip-inflight names the kind, new #chip-last; tail drawer + btn-tail removed; Lead panel keeps streamed lines (LEAD.lines) \u2014 live box refilled on re-render, finished turn shows `activity (N lines)` <details>; gate line names the cycle turn (\"<agent> is on its cycle turn (round N) \u2014 see Cycle activity\"); shared buffered EventSource registry (one connection per resource, late joiners get the replay); region shown when phase \u2228 pending launch \u2228 in-flight \u2228 any activity.\n- docs \u2014 how-tagteam-works #cockpit (Cycle region + Activity, outcome vocabulary, endpoints) and #lead (nothing collapses); README cockpit section + cockpit-cycle.png; docs/cockpit-issues.md 2026-08-16 entry marked resolved; docs/media/README.md manifest; plan doc \"Implementation notes (impl round 1)\" lists every deviation.\n- scripts/showcase_seed.py \u2014 demo-web gets a running reviewer turn (detached sleep as the live pid, growing log, gate pass, three finished turns).\n- tests/test_cockpit_activity.py (new, 46 tests) \u2014 vocabulary, every source + normalisation, marker merge / dedupe / same-id record, cap, last_turn, now keys, launch_view (pending\u2192turn, failed recent+current intent, orphaned), signature sources + bounded cost, tail?stem= + traversal, /api/activity + legacy 404, log SSE (stream, live follow, partial-line hold/flush, end, Last-Event-ID replay, :end cursor, finished stem, 400/404, cap counts), lead SSE on the shared poller, JS/HTML/CSS source guards (no innerHTML in the Phase 43 block, ids present, tail gone, OUTCOME_LABEL exactly seven). tests/test_docs_story.py registers the screenshot.\n\nVerified: focused runs green while working; manual Playwright walk (1280\u00d7800) recorded in the plan doc \u2014 running reviewer turn streams; turn ended cancelled and finished \u2192 same row patched with lines kept incl. the final partial line; Start \u2192 starting \u2192 ok (Start card gone/back, Lead panel keeps activity (4 lines)); reload persistence. Full suite: the gate on this submission.\n\nNo schema bump, no CLI change, saloon/hub untouched. Scoping: saloon rethink is Phase 44 (approved in the plan round).", "ts": "2026-08-18T00:38:00.308703+00:00"}
{"round": 1, "role": "gatekeeper", "action": "GATE_PASS", "content": "GATE: PASS | scope 13 paths | plan-doc ok | tests ok (1392 passed, 5 skipped, 4m15s)\nchecked: HEAD f341913", "ts": "2026-08-18T00:42:15.717989+00:00", "updated_by": "Gatekeeper", "gate_event": "cockpit-hardening/impl/r1/171", "gate_id": 5, "gate_attempt": 1}
{"round": 1, "role": "reviewer", "action": "REQUEST_CHANGES", "content": "The gate's full suite is green (1392 passed, 5 skipped @ f341913), and the focused Phase 43 suite also passes (46 passed). One correctness issue remains in the Activity log ordering.\n\n`upsertActivity()` only calls `insertActRow()` for a newly created row or when a terminal row becomes running. When a running row becomes terminal, it patches the row but does not update `row.dataset.key` or reinsert it. Since the ordering rule is running first, then newest first, a long-running turn can remain above newer finished rows after it ends. That violates the documented `/api/activity` ordering in the rendered log and can leave old terminal activity at the top until reload.\n\nPlease reapply `insertActRow(list, rec)` (or equivalent reorder/key refresh) whenever an existing row's ordering-relevant status or timestamp changes, and add a focused JS/source or behavioural regression test covering running \u2192 terminal reordering without rebuilding the activity container.", "ts": "2026-08-18T00:43:35.095721+00:00"}
{"round": 2, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Round 2 \u2014 addresses reviewer r1 (Activity log ordering). Branch phase-43-cockpit-hardening @ 0c98329 (PR #21). gate: see entry @ 0c98329.\n\n- tagteam/data/web/cockpit.js `upsertActivity`: an existing row is re-inserted (`insertActRow`, same node moved via insertBefore \u2014 lines and stream stay with it, container never wiped) whenever its sort key `running?|started_at|id` changes: running \u2192 terminal, terminal \u2192 running, or a corrected timestamp. The old branch that re-sorted only on terminal \u2192 running is gone; the \"keep the stream open until its own `end`\" behaviour is unchanged.\n- tests/test_cockpit_activity.py `TestActivityLogBehaviour` (2 new; 48 total): (1) behavioural \u2014 the real Phase 43 block is evaluated in `node` under a ~40-line DOM stub (createElement/textContent/classList/dataset/appendChild/insertBefore/children) and driven through: running turn + newer finished turn (running on top) \u2192 the running one ends with the same id \u2192 it moves BELOW the newer finished row \u2192 a newer running row appears on top \u2192 a terminal \u2192 running flip moves up; asserts DOM order at each step, same node identity, the two appended lines intact, 3 rows in the DOM / 3 in ACT.rows. Skipped when `node` is absent (the repo has no JS harness; this is the one node-executed test). Verified to FAIL on the pre-fix JS (both tests) and pass on the fix. (2) source guard: `upsertActivity` contains `actSortKey(it)` + `insertActRow(list, rec)`.\n- docs/phases/cockpit-hardening.md: status line + \"Impl round 2\" note.\n\nFull suite: the gate on this submission.", "ts": "2026-08-18T00:45:33.271282+00:00"}
{"round": 2, "role": "gatekeeper", "action": "GATE_PASS", "content": "GATE: PASS | scope 13 paths | plan-doc ok | tests ok (1394 passed, 5 skipped, 4m22s)\nchecked: HEAD 0c98329", "ts": "2026-08-18T00:49:54.967494+00:00", "updated_by": "Gatekeeper", "gate_event": "cockpit-hardening/impl/r2/173", "gate_id": 6, "gate_attempt": 1}
{"round": 2, "role": "reviewer", "action": "APPROVE", "content": "Approved. Round 2 correctly reorders changed activity rows by moving the existing keyed node, preserving stream state and lines. The new Node-driven regression covers running-to-terminal and terminal-to-running ordering. Gate: 1394 passed, 5 skipped @ 0c98329; reviewer focused suite: 48 passed.", "ts": "2026-08-18T00:50:40.669262+00:00"}
