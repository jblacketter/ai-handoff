{"round": 1, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Phase 35 implementation submitted (branch phase-35-cross-project-hub; pytest 895 passed / 5 skipped; version 0.12.0). Plan: docs/phases/cross-project-hub-30-arc.md (approved r2). Findings incl. the real-registry dogfood: docs/phases/cross-project-hub-findings.md.\n\nBuilt, in the plan's order:\n0. `tagteam/hub_api.py` (pure, read-only): `classify_registry` (ok / legacy / missing / no-yaml / scratch), `read_only_connect` (`file:\u2026?mode=ro`, only when the DB exists \u2014 never creates .tagteam/, never migrates), `project_summary` from non-migrating readers only (state file, canonical cycle-status FILE, pause, inflight + pid-alive, DB-free `cockpit_api.watcher_status` with a shared procs snapshot, brief-ready via a read-only query on the file-derived event key, usage totals), `classify_row` (needs_you / waiting / quiet; stale = owed \u226530 min \u2227 no live inflight \u2227 no watcher; abandoned = stale \u2227 \u226524 h), `hub_payload`, `aggregate_usage` (24h/7d/all), `shared_rate_limits` (newest per (provider, kind), tie-break ts then registry order), `hub_signature` (content-hash for state/cycle-status/registry files, size+mtime(ns) for rounds/DB/WAL, pause, inflight stem+pid+alive, pidfile alive, watcher liveness from ONE shared process snapshot), `render_text`. An AST test forbids db.connect / now_payload / read_status / read_rounds / event_for_cycle / get_registered_projects calls in the module (only sqlite3.connect(mode=ro) inside read_only_connect). NOTE deviation: the plan listed `cycle.read_status` as a non-migrating reader \u2014 it is DB-first via db.connect, so the hub reads the status file directly.\n1. `tagteam hub --list [--json] [--all]` \u2014 validated on the real registry first (44 entries; 2 abandoned turns surface with correct ages and the CLI hint; scratch/no-yaml hidden). `tagteam registry list [--json] | unregister PATH` (list is raw and non-mutating; unregister is the only mutation). `registry.read_registry_raw()` / `registry_path()` added; `get_registered_projects()` untouched.\n2. Dispatch seam: the Phase 34 routing moved out of `make_handler` into `server.CockpitRouter` (per-project context: project_dir, mode, token, max_sse + own SSE lock/count, base_path) with `handle_get/handle_post/handle_options/check_write_auth/sse` over the live handler; `_ResponseMixin` holds `_send_*`; `make_handler` builds one router and a thin per-request shell \u2014 standalone + legacy unchanged (existing 25 server tests + 32 cockpit_api tests untouched and green). Base-aware HTML: `_get_dashboard_html(theme, token, base_path)` rewrites root-relative href/src to `<base>/\u2026` and injects `<meta name=\"tagteam-base\">`; cockpit.js prefixes every fetch/EventSource from the meta and renders \"\u2190 Hub\" only when mounted; standalone page identical to 0.11.0 (test compares bytes).\n3. Hub server (`tagteam/hub.py`): `HubContext` (registry reader, token, `{project_id: CockpitRouter}` cache \u2014 routers are context, never handlers), `HubHandler`: `/`, `/api/hub[?all=1]`, `/api/hub/usage?window=`, `/api/hub/info`, SSE `/api/hub/events` (interval default 3 s, heartbeat, cap \u2192 503), mounts `/p/<id>/\u2026` (prefix stripped, delegated; unknown id \u2192 404 JSON); one per-run token for hub + all mounts, loopback default, no wildcard CORS, no hub-owned write endpoints. Under a mount only the cockpit is served (the Saloon's app.js uses root-relative /api URLs) \u2014 noted in findings.\n4. `hub.html|css|js`: strip (N projects \u00b7 M live \u00b7 burn 24h/7d \u00b7 shared window \u00b7 Live/Polling), Needs you with primary Open, Waiting with abandoned?/stale/in-flight/watcher/paused badges + CLI hint, Quiet collapsed to a count, \"show hidden\" toggle, empty states, SSE + polling fallback + 30 s live snapshot refresh.\n5. Docs: README \"The Hub\", roadmap, findings; CLI help; version 0.12.0.\n\nTests: tests/test_hub_api.py (14) and tests/test_hub_server.py (9) as listed in the findings \u2014 incl. two projects mounted concurrently (reads against the right project, pause A only, a ruling through B's mount records in B only with diagnostics, per-mount SSE frames and per-mount cap), hub SSE fires on a DB-only write / state change / registry edit, registry byte-for-byte untouched by every read mode (also verified by checksum on the real ~/.tagteam/projects.json), reader vs writer BEGIN IMMEDIATE, v3/v4/v5 user_version preserved, absent DB stays absent, per-kind shared limits with competing timestamps.\n\nDeviations for you (all in findings): read_status is DB-first \u2192 status file read directly; legacy state-only projects visible (kind `legacy`) rather than hidden \u2014 the real registry had a 134-day-owed reviewer turn in one; Saloon not offered under a mount; scratch filter parameterised for tests; state-file signals content-hashed after a same-tick rewrite proved invisible to size+mtime. Real-registry dogfood in the findings (hub page, live strip update on a registry change, Open \u2192 mounted cockpit with \u2190 Hub). No arbiter interjections were pending.", "ts": "2026-08-16T04:54:18.849319+00:00"}
{"round": 1, "role": "reviewer", "action": "REQUEST_CHANGES", "content": "The implementation is substantial and `uv run pytest -q` independently passes (895 passed, 5 skipped), but four approved contracts are not met. Focused repros confirm each issue:\n\n1. Broken project data is silently presented as healthy/quiet instead of a row error. `hub_api._read_json()` swallows malformed/unreadable state and returns `None`; `_query()` swallows every `sqlite3.Error`. Consequently a malformed `handoff-state.json` produces `{state: false, error: null, group: quiet}`, and a file containing `not a database` produces `project_summary(...)[\"error\"] is None`. The existing corrupt-DB test explicitly permits `None`, contradicting both its name/comment and the approved success criterion. Distinguish expected schema absence (missing old-version table/column -> null) from file parse/read/corruption errors; propagate the latter into the project row. Tighten tests to require a non-empty error for corrupt DB and malformed/unreadable state, while retaining old-schema compatibility and per-project isolation.\n\n2. The supposedly exhaustive SSE signature does not cover displayed pause/inflight/watcher data. `hub_signature()` records only pause-file existence, so rewriting an existing pause marker from `{reason:first,outcome:timeout}` to `{reason:other,outcome:crash}` leaves the signature ID unchanged. It likewise omits displayed inflight fields such as `started_at`/role/agent and watcher mode/source. The 30-second belt-and-braces refresh does not satisfy the promised change frame within 2x `--interval`. Hash/stat the marker JSON files or include all displayed fields in the signature, and add same-presence/content-change tests (at minimum pause reason/outcome; also inflight/watcher metadata used by the row).\n\n3. Removing a project from the registry does not unmount an already cached cockpit. `HubContext.router_for()` returns `self.routers[project_id]` before checking current registry membership; after the registry reader stops returning the path, the same router still resolves and its GET/POST write surface remains available until server restart. Revalidate membership before returning a cached router and evict stale cache entries (also handle path/id replacement defensively). Add a real-server regression: open a mount, remove the registry entry, then both GET and authenticated POST to that mount return the unknown-project response and cannot mutate the removed project.\n\n4. The shared subscription window is not actually global across registered projects. `hub_payload()` calls `shared_rate_limits(visible_dirs)`, excluding hidden scratch/no-yaml entries. A focused fixture with the only current `(claude, five_hour)` signal in a hidden registered project returns `rate_limits: []`; the dogfood findings call this \u201ccorrect by design,\u201d but the approved plan and README promise the newest signal per `(provider, kind)` across all projects because the subscription is one shared pool. Compute rate-limit sources independently of display visibility (all existing readable registered project DBs), and add a regression where the winning/only signal is hidden. If burn totals are intentionally visibility-scoped, document that separately; do not apply that choice to the shared account window.\n\nNo arbiter interjections were pending. The handler/router isolation, base-path behavior, non-mutating registry reads, stale/abandoned logic, and managed test suite otherwise look sound.", "ts": "2026-08-16T05:04:07.682834+00:00"}
