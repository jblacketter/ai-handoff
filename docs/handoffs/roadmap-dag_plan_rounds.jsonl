{"round": 1, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Phase 40 plan: Roadmap as a DAG (3.4.0). See docs/phases/roadmap-dag.md. Optional '- **Depends on:** \u2026' line per roadmap phase (slug / Phase N / exact name; unknown/self/cycle are errors listed by 'roadmap check'); stable-Kahn topological 'roadmap queue' (edge-free roadmaps byte-identical to 3.3.0); 'roadmap graph [--mermaid]', 'roadmap ready [--json]', 'roadmap phases' gains a depends_on column only when present; full-roadmap advance picks the next READY queue entry (re-parses roadmap), never starts a blocked phase \u2014 sets roadmap.pause_reason + turn:lead command, 'roadmap resume' re-runs the advance; new tagteam/worktree.py: 'roadmap worktree <phase> [--from REF] [--remove [--force]]' creates ../<repo>-<phase> on branch phase-<slug>, seeds framework files, registers it as an ordinary project (registry format unchanged; metadata in ~/.tagteam/worktrees.json), prints kickoff; 'roadmap worktrees [--json]' lists path/branch/phase/state/merged; refuses not-ready/unknown/duplicate; --remove refuses unmerged without --force. Two worktrees = two projects (independent state, watcher, gate, panel). Out: two phases in one root, auto-merge, cockpit UI. Tests: parsing/validation/topo (diamond, ties, start semantics), watcher blocked/resume, worktree create/list/remove in temp git repos, flag-off byte-identical, SKILL copies identical. Open questions: sibling dir vs .worktrees/; 'roadmap resume' vs 'tagteam resume'; require ready vs not-terminal for worktree creation.", "ts": "2026-08-17T00:31:01.953104+00:00"}
{"round": 1, "role": "reviewer", "action": "REQUEST_CHANGES", "content": "The overall direction is sound, and I agree with the proposed answers to the open questions: use sibling worktrees, keep `roadmap resume` separate from the existing dispatch resume, and require graph readiness before worktree creation. Please revise the plan to close these correctness gaps before implementation:\n\n1. Define one authoritative dependency-satisfaction rule across commands and the watcher. Today an impl APPROVE only adds the phase to `state[\"roadmap\"][\"completed\"]`; it does not make that phase terminal in `docs/roadmap.md`. As written, re-parsing the roadmap after an approval can therefore leave every dependent blocked forever. Specify how `ready`, full-roadmap advance, and worktree creation combine persisted roadmap dispositions with the active run's completed list. Also specify the cross-worktree publication contract: e.g. the phase branch must commit a terminal roadmap disposition and that commit must be merged before another project can observe it. Add tests for both same-run approval unblocking a dependent without a manual roadmap edit and merged cross-worktree completion unblocking it.\n\n2. Resolve the contradiction in `queue [start]`. The plan says nonterminal predecessors are \u201cassumed done,\u201d while also promising that full-roadmap mode never starts a blocked phase. A requested start whose dependency is nonterminal cannot silently bypass that edge. Prefer either including every nonterminal dependency ancestor before the requested phase, or rejecting the start with the blockers; retain the old suffix behavior only for edge-free roadmaps. Pin the chosen behavior in CLI and watcher tests.\n\n3. Make dynamic advancement explicit. A stored topological queue can become stale when another worktree finishes and merges a queued phase. On every impl advance/resume, skip entries that are already terminal or recorded complete, advance `current_index` to the phase actually selected, and produce `roadmap-complete` when no incomplete entries remain. Add the parallel diamond case where a middle queue entry completes externally before the main watcher reaches it.\n\n4. Validate graph identities before building the slug-keyed dict. Duplicate phase numbers and duplicate normalized slugs (including different names that slugify identically) make references ambiguous and currently risk silent dict overwrite / branch collision. `roadmap check`, `queue`, `ready`, and worktree creation should fail with all such identity errors; add tests.\n\n5. Define the worktree merge target used by `merged?` and safe removal. Record the parent/integration branch (and base ref when `--from` is used) at creation, then check whether the phase branch is merged into that recorded target rather than whichever branch happens to be checked out later. Test parent branch switching before list/remove.", "ts": "2026-08-17T00:32:41.666884+00:00"}
{"round": 2, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Round 2 \u2014 all five points addressed in docs/phases/roadmap-dag.md (Design \u2192 \"One dependency-satisfaction rule\", \"Full-roadmap mode\", \"Worktrees\"; Scope A/B/E; Success criteria 2\u20135; open questions moved to \"Decisions\" as agreed: sibling dir, separate `roadmap resume`, require ready).\n\n1. One rule: `satisfied(dep) := is_terminal_status(roadmap disposition on disk) OR normalize_phase_key(dep) in state.roadmap.completed (active run)`. Used identically by `roadmap ready` (reads the project's handoff-state when run_mode=full-roadmap, prints \"(+ N completed in the active run)\", `--roadmap-only` ignores it), the watcher advance/`roadmap resume`, and `roadmap worktree`. So an impl APPROVE that only appends to `completed` (unchanged behaviour) unblocks dependents in the same run \u2014 tested. Cross-worktree publication contract spelled out: another project observes completion only through docs/roadmap.md in its own checked-out tree \u2192 phase branch commits a terminal `Status:` disposition and is merged into the recorded target; siblings see it after merging/rebasing that branch; until then dependents stay blocked there by design. Tests: same-run unblocking without a roadmap edit; temp repo worktree commits terminal status \u2192 merged \u2192 parent's `ready` unblocks (and not before).\n\n2. `queue [start]` no longer \"assumes done\": the queue = {start} \u222a incomplete phases after start \u222a every nonterminal, not-completed dependency ancestor of those (transitively), topologically ordered \u2014 unmet ancestors are pulled in AHEAD of start and reported on stderr; unneeded predecessors are dropped as today; edge-free roadmaps \u2192 today's suffix byte-identical. Pinned in CLI tests and a watcher `--roadmap [start]` test.\n\n3. Dynamic advance made explicit as five steps on every advance/resume: re-parse (invalid \u2192 pause \"roadmap invalid: \u2026\"); walk queue[current_index+1:] skipping entries terminal on disk or in `completed` (logged, not started, `completed` stays \"approved in this run\"); select the first entry whose deps are satisfied and set current_index = THAT index; no incomplete entry left \u2192 roadmap-complete; incomplete but all blocked \u2192 pause_reason \"blocked: <phase> depends on <deps>\", turn:lead, no start; `roadmap resume` re-runs the same function through update_state(expected_seq). Tests: diamond A\u2192{B,C}\u2192D with B completed externally while on A \u2192 skip B, select C (index 2), then D; all-blocked \u2192 pause + resume; externally-completed tail \u2192 roadmap-complete.\n\n4. `validate_identities(phases)` runs before any slug-keyed dict: duplicate heading numbers, duplicate normalized slugs (different names slugifying identically), empty names \u2014 all problems listed in one RoadmapGraphError; refused by check/queue/ready/graph/worktree creation, and the advance pauses on it. Applies to edge-free roadmaps too (a duplicate slug was already a latent bug), so the byte-identical guarantee is stated for well-formed edge-free roadmaps \u2014 this repo's 40-phase roadmap and all fixtures qualify (verified: no duplicate numbers/slugs; a test asserts it). `parse_roadmap` itself stays lenient so `roadmap phases` output is unchanged.\n\n5. Sidecar records `target` (parent's checked-out branch at creation, or `--target BRANCH`) and `base` (resolved sha of HEAD/`--from`). `merged?` = `git merge-base --is-ancestor <branch> <target>` in the parent repo against refs/heads/<target>, falling back to origin/<target> if the local branch is gone (squash-merge + local delete); neither \u2192 \"target missing\" = unmerged. `--remove` refuses unmerged/missing-target without `--force`. Test switches the parent to another branch between creation and list/remove.", "ts": "2026-08-17T00:35:05.037991+00:00"}
{"round": 2, "role": "reviewer", "action": "REQUEST_CHANGES", "content": "Round 2 resolves all five prior findings and the three design decisions are now clear. Two correctness gaps remain in the revised design:\n\n1. Do not lose blocked entries when selecting a later ready entry. The proposed advance scans only `queue[current_index+1:]` and then sets `current_index` to the selected entry's index. If B is blocked but later independent C is ready, selecting C permanently puts B before `current_index`; no later advance can reconsider B. This can occur after a mid-run roadmap edit or with an externally completed prerequisite\u2014the exact dynamic cases the design claims to support. Either (a) pause at the first incomplete blocked entry and never jump over it, or (b), preferably for DAG parallelism, scan the entire selected queue on every advance and exclude only entries terminal on disk, in `completed`, or currently active; `current_index` may describe the current selection but must not define the remaining set. Then B can run after C once unblocked. Define roadmap-complete over the selected queue, not the suffix. Add a mixed case: A approved, B blocked, C ready \u2192 run C; after C and B's prerequisite become satisfied \u2192 run B; then finish without rerunning A/C.\n\n2. Ensure a worktree's base actually contains every dependency used to declare it ready. The active-run `completed` list can satisfy a dependency while its approved implementation is still an uncommitted working-tree change; `git worktree add ... HEAD` (and especially an arbitrary `--from REF`) would then start the dependent from code that does not contain that dependency. Define and enforce the publication boundary for worktree creation. A minimal safe contract is: require the parent worktree clean; when readiness relies on active-run `completed`, require the creation base to be the clean current HEAD or a descendant of it (reject an older/divergent `--from`); document that approved phases must be committed before spawning dependents. Alternatively record completion commit SHAs and verify ancestry. Add refusal tests for dirty approved dependency changes and for `--from` predating/diverging from the completion base.\n\nOne small implementation test should also ensure an empty `### Phase N:` heading is actually parsed/reported as an identity error; the current `(.+)` heading regex would otherwise skip it before `validate_identities` can see it.", "ts": "2026-08-17T00:36:21.495518+00:00"}
{"round": 3, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Round 3 \u2014 both gaps closed + the heading test, in docs/phases/roadmap-dag.md.\n\n1. Advance never loses a blocked entry: step 2 now computes `remaining` over the WHOLE selected queue (every entry not terminal on disk, not in `completed`, not the phase just approved) \u2014 `current_index` only describes the current selection and never defines the remaining set. Step 3 selects the first entry of `remaining` in queue order (queue is topological, so order is a valid priority) whose deps are satisfied and sets `current_index` to that index, which may be LOWER than before. roadmap-complete is defined as `remaining` empty (over the selected queue, not the suffix). Mixed test added: A approved, B blocked, C ready \u2192 C (index 2); B's dep satisfied; after C \u2192 B (index 1); after B \u2192 roadmap-complete without re-running A/C.\n\n2. Worktree publication boundary (Design \u2192 Worktrees \u2192 \"Publication boundary for creation\"): (a) parent must be clean \u2014 no modified/staged tracked files, no untracked non-ignored files \u2014 refuse listing the paths, no override flag (the fix is a commit); (b) `base` = resolved `--from REF` or HEAD, and readiness is evaluated on the roadmap AS OF `base` (`git show <base>:docs/roadmap.md`), so an on-disk terminal disposition counts only if that commit carries it; (c) the active run's `completed` may satisfy a dep only if `base` contains HEAD (`git merge-base --is-ancestor HEAD <base>` \u2014 HEAD or a descendant); older/divergent `--from` with a `completed`-satisfied dep is refused with the reason. `base` sha recorded in the sidecar; HTW/README state that approved phases must be committed (and published per the contract) before spawning dependents. Tests: dirty parent (modified tracked; untracked) refuse; `--from` older + completed-satisfied dep refuse; `--from` descendant ok; no `--from` ok; dep terminal at HEAD but not at older `--from` refuse; terminal at both ok.\n\n3. Empty heading: `validate_identities` takes the raw roadmap text and runs its own lenient heading scan (`^###\\s+Phase\\s+(\\d+):\\s*(.*)$`) so a bare `### Phase N:` (skipped by parse_roadmap's `(.+)`) and a whitespace-only name are both reported as `phase N: empty name`; test covers both spellings. `parse_roadmap` stays lenient/unchanged.", "ts": "2026-08-17T00:39:23.154298+00:00"}
{"round": 3, "role": "reviewer", "action": "APPROVE", "content": "Approved. Round 3 closes the remaining correctness gaps: whole-queue advancement reconsiders blocked entries and can move current_index backward without rerunning completed phases; roadmap-complete is defined over the selected queue; worktree creation enforces a clean parent plus roadmap-at-base readiness and rejects bases that omit active-run completed dependencies; empty headings are caught by raw-text identity validation. The plan now has explicit contracts and tests for the DAG, watcher, and worktree boundaries.", "ts": "2026-08-17T00:40:18.706226+00:00"}
